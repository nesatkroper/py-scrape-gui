#!/bin/bash


#Ubuntu
pip install pyinstaller

pyinstaller --onefile --windowed pyscrape.py

mkdir -p pyscrape_deb/DEBIAN
mkdir -p pyscrape_deb/usr/local/bin

cp dist/pyscrape pyscrape_deb/usr/local/bin/
nano pyscrape_deb/DEBIAN/control


Package: pyscrape
Version: 1.0
Section: utils
Priority: optional
Architecture: amd64
Maintainer: Your Name <you@example.com>
Description: PyScrape Python GUI App

dpkg-deb --build pyscrape_deb


#Install 
# Install the .deb file
sudo dpkg -i pyscrape_deb.deb

# If you get dependency errors, run:
sudo apt-get install -f


#or

sudo apt install ./pyscrape_deb.deb


#MacOS
pyinstaller --onefile --windowed app/app.py

hdiutil create -volname PyScrape -srcfolder dist/app.app -ov -format UDZO PyScrape.dmg

https://infinity-rich88.online
assets/images/logo.png this 



sudo apt install ./nun-info_1.0_all.deb
sudo apt -f install
